L = [3, 5, 4] ; L = L.sort()
x, y = (1, 2)
X = 1, 2, 3 ; X[1] = 4
X = [1, 2, 3] ; X[3] = 4
X = "abc" ; X.append("d")
map(pow, range(8))

# Zadanie 2
# W kodzie tym próbuje się zapisać trzy wartości do dwóch zmiennych
# Dodatkowo te zmienne zapisuje się jako krotki a potem próbuje je zmienić